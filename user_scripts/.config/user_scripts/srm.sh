SRM_UNDELETABLES=(
    "$HOME"
    "$HOME/downloads"
    "$HOME/documents"
    "$HOME/images"
    "$HOME/dev"
    "$HOME/Videos"
    "$HOME/.config"
    "$HOME/.dotconfig"
    "$HOME/.local"
    "${XDG_HOME_BIN}"
    "$HOME/UNDELTEST"
)
